# Local environment configuration for ROS 2 builds
# Copy this file to .env and customize for your machine
# The .env file is gitignored and will not be committed

# Number of parallel workers for colcon builds and tests
# Limits concurrent package compilation to reduce memory/CPU usage
PIXI_PARALLEL_WORKERS=2
